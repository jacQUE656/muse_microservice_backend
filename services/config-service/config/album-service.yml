server:
  port: 1001

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/${ALBUM_DB}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver