server:
  port: 10001

spring:
  datasource:
    url: jdbc:postgresql://muse-postgres:5432/${ALBUM_DB}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver