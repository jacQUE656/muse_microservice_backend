server:
  port: 8761  # Conventional Eureka Dashboard Port

eureka:
  client:
    register-with-eureka: false  # The server doesn't need to register with itself
    fetch-registry: false         # The server doesn't need to fetch its own registry