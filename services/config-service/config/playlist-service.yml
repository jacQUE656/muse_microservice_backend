server:
  port: 1004

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/${PLAYLIST_DB}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver