spring:
  application:
    name: playlist-service  # Tells config-service to serve 'playlist-service.yml'
  config:
    import: "optional:configserver:http://localhost:8888" # Points to your centralized server